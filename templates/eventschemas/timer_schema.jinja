def to_cloudevents_schema(event):
    utc_timestamp = datetime.datetime.utcnow().replace(tzinfo=datetime.timezone.utc).isoformat()
    return {'id':               context.invocation_id,
            'source':           context.function_directory,
            'specversion':      '1.0',
            'type':             'com.azure.functions.timerTrigger',
            'datacontenttype':  'functions.TimerRequest',
            'time':             utc_timestamp,
            'data':             event}
{% if data.is_loop %}event['{{ data.task_name }}'] = []
{% include 'indentation.jinja' %}{% include 'loop.jinja' %}
{% include 'indentation.jinja' %}    queue_client = QueueServiceClient.from_connection_string({{ data.connection_string }})
{% include 'indentation.jinja' %}    event['{{ data.task_name }}'].append(queue_client.list_queues({{ data.args[0] }}))
{% else %}queue_client = QueueServiceClient.from_connection_string({{ data.connection_string }})
{% include 'indentation.jinja' %}event['{{ data.task_name }}'] = queue_client.list_queues({{ data.args[0] if data.args[0] }}){% endif %}